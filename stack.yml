services:
  web:
    image: ${DOCKERHUB_USERNAME}/swarm-demo:latest
    ports:
      - "80:8080"
    deploy:
      replicas: 6
      restart_policy:
        condition: any
      update_config:
        parallelism: 2
        delay: 10s
      placement:
        max_replicas_per_node: 3
